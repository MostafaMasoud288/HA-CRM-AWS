---
- name: Join k3s server to existing cluster
  ansible.builtin.shell: |
    curl -sfL https://get.k3s.io | K3S_TOKEN={{ k3s_token_value }} sh -s - server \
      --server https://{{ groups['masters'][0] }}:6443
  args:
    creates: /usr/local/bin/k3s
